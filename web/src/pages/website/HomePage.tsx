import { LandingExperience } from "../../components/landing/LandingExperience";

/** Legacy React Router portal entry — mirrors the TanStack landing. */
export function HomePage() {
  return <LandingExperience />;
}

export default HomePage;
